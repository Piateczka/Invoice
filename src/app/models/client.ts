export interface Client {
    clientId: number;
    name: string
    street: string
    premiseNumber: string
    buildingNumber: string
    postalCode: string
    city: string
    country: string
    nip: string

}